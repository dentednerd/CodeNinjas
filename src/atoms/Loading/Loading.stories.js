import React from 'react';
import Loading from '.';

export default {
  title: 'atoms/Loading',
  component: Loading,
}

const Template = () => <Loading />;

export const Default = Template.bind({});
